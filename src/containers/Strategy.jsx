import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

const Power = () => <div>Strategy</div>;

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(Power);
